#include "main.h"

/**
  * _abs - This function computes the absolute value.
  * @n: integer
  *
  * Return: absolute value of n
  */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
