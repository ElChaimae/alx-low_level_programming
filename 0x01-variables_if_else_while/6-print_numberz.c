#include<stdio.h>

/**
 * main - Entry point 
 *
 * Return: returns 0 when executed successfully
 */
int main(void)
{
	int i;
	for (i='0'+0;i<'0'+10;i++)
		putchar(i);
	putchar('\n');	
	return (0);
}
